import logging

logging.basicConfig(format='[%(asctime)s]: %(message)s | %(levelname)s on LINE:%(lineno)d in %(filename)s',
                    level=logging.INFO,
                    # level=logging.DEBUG,
                    )
